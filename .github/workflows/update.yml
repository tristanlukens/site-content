name: updateList

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
    
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      
      - name: test
        run: pwd

      - name: update
        run: ls cover post > content.txt
        shell: bash
